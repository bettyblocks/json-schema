{
  "$id": "/schemas/actions/function/options/base.json",
  "title": "Base",
  "description": "The base properties that are required for all options.",
  "type": "object",
  "properties": {
    "type": {
      "enum": [
        "Boolean",
        "Integer",
        "Model",
        "Record",
        "String"
      ]
    },
    "io": {
      "enum": ["in", "out"]
    }
  },
  "required": ["type", "io"]
}