{
  "$schema": "http://json-schema.org/draft-07/schema",
  "$id": "https://raw.githubusercontent.com/bettyblocks/json-schema/master/schemas/actions/function/branch.json",
  "title": "Branches",
  "description": "Branches that can be configured for a ActionStep. All the available types of options are found in the definitions.",
  "type": "object",
  "additionalProperties": false,
  "properties": {
    "min": {
      "type": "number"
    },
    "max": {
      "type": "number"
    },
    "filterOption": {
      "properties": {
        "meta": {
          "properties": {
            "type": {
              "enum": ["Value"]
            }
          },
          "oneOf": [
            {
              "$ref": "/bettyblocks/json-schema/master/schemas/actions/function/option/types/value.json"
            }
          ],
          "required": ["type"]
        }
      },
      "required": ["meta"]
    },
    "values": {
      "type": "array",
      "items": {
        "additionalProperties": false,
        "type": "object",
        "properties": {
          "label": {
            "type": "string"
          }
        },
        "required": ["label"]
      }
    }
  },
  "required": ["min", "max", "filterOption", "values"]
}
