{
  "$schema": "http://json-schema.org/draft-07/schema",
  "$id": "/schemas/actions/function/option/base.json",
  "title": "Base",
  "description": "The base properties that are required for all options.",
  "type": "object",
  "properties": {
    "type": {
      "description": "The type of option you want to supply. Examples are: Boolean, Integer and Model.",
      "enum": ["Boolean", "Integer", "Model", "Record", "String"]
    },
    "io": {
      "description": "Determines if this option is input or output. Output should be supplied in the function implementation.",
      "enum": ["in", "out"]
    }
  },
  "required": ["type", "io"]
}
